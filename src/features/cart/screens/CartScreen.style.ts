import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        padding: 16,
        gap: 16
    },
    purchaseButton: {
        backgroundColor: '#EC5B13',
        borderRadius: 16,
    },
    purchaseText: {
        color: '#fff',
        fontSize: 16,
        fontWeight: 700,
        padding: 16,
        textAlign: 'center'
    }
})